package app.webca.codingtest._common;

/**
 * <pre>
 * Map : 키와 값으로 데이터 조작
 *  - Hashtable : 동기화 보장
 *  - HashMap : 중복(X), 정렬(X), 동기화 보장하지 않음
 *   - LinkedHashMap : HashMap 대비 수정/삽입 빠름, 검색 느림
 *  - WeakHashMap : 중복(X), 정렬(X), 참조가 약한 값들을 GC 가 자동으로 삭제. 오류 발생 가능성.
 *  - IdentityHashMap : 중복(X), 정렬(X), 참조 변수를 통한 값의 비교. 자주 사용되지 않음.
 *  - SortedMap<I> : 정렬
 *   - TreeMap : 중복(X), 정렬(O)
 * </pre>
 */
public class MapUtil {

    public static void main(String[] args) {
    }
    
}
